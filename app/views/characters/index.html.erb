<% @characters.each do |character| %>
    <h1><a href="/characters/<%= character.id %>"><%= character.name %> </a></h1>
    <% character.character_klasses.each do |klass| %>
        <h2>Level <%= klass.level%> <%= Klass.find(klass.klass_id.to_i).name.capitalize %> </h2>
    <%end%>
<%end%>
